runtime: python311
entrypoint: gunicorn -b :$PORT app:app --timeout 120 --workers 1

instance_class: F1

automatic_scaling:
  target_cpu_utilization: 0.65
  min_instances: 1
  max_instances: 10

handlers:
  - url: /static
    static_dir: static
  - url: /.*
    script: auto
    secure: always

env_variables:
  GOOGLE_CLOUD_PROJECT: techandethics
  GMAIL_APP_PASSWORD: "ppoe onud hlfu pace"
  STORAGE_BUCKET: "tech-ethics-club-uploads"

service_account: tech-ethics-club-sa@techandethics.iam.gserviceaccount.com 